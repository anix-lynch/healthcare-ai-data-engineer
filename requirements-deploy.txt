fastapi>=0.115
uvicorn[standard]>=0.27
pydantic>=2.0
pandas>=2.0
starlette>=0.40
rank_bm25>=0.2.2
# /api/ask grounded answer path — Gemini via Vertex AI (runtime SA auth).
vertexai>=1.60
google-cloud-aiplatform>=1.60
