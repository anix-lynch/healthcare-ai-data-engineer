fastapi>=0.115
uvicorn[standard]>=0.27
pydantic>=2.0
pandas>=2.0
rank_bm25>=0.2.2

# Grounded RAG answer path (/api/ask) — Gemini via Vertex AI.
# Runtime auth = Cloud Run service identity (Vertex AI User); local = SA key / ADC.
vertexai>=1.60
google-cloud-aiplatform>=1.60
# fastapi>=0.115 + starlette>=0.40 required: newer httpx 0.28+ TestClient
# uses ASGITransport(app=...) instead of TestClient(app=...) — older fastapi
# versions break with the new httpx signature.
starlette>=0.40

# dbt (Fabric target) — optional for portfolio browse; needed for `dbt run`
# dbt-core>=1.7
# dbt-fabric>=1.7

# Enrichment (Vertex AI) — optional, needs GCP auth
# google-cloud-aiplatform>=1.60
# vertexai>=1.60

# ML pipeline (MLflow + XGBoost) — optional
# mlflow>=2.9
# xgboost>=2.0
# scikit-learn>=1.4

# Tests
pytest>=7.4
httpx>=0.27
google-cloud-pubsub>=2.21  # producer side: publish_event.py → encounter-events topic
